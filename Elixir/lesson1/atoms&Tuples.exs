# Atoms are data types that have the VALUE of their own NAME.
# they are represented by ":"

x = :ok
IO.puts(x)

tuple = {:ok, 152}
File.read("./text.txt")
# the ":ok" atom indicates in tuples that
# the operation went ok!
